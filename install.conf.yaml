- defaults:
    link:
      relink: true
      create: true
    shell:
      stdout: true
      stderr: true

- clean: ['~']

- link:
    ~/.gitconfig:
    ~/.config: config/
    ~/.config/pikaur.conf: config/pikaur/pikaur.conf  
- create:
    - ~/documents
    - ~/downloads
    - ~/pictures
- shell:
  - [git submodule update --init --recursive, Installing submodules]
  - [sudo cp scripts/* /usr/local/bin]
